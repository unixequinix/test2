.wrapper-store-module
  h3.center-text
    = current_event.name
    = t('checkout.store')
  p = t('checkout.description')
<<<<<<< HEAD

= form_tag event_checkouts_path(current_event), method: :post do |f|
  - @checkout_presenter.preevent_products.each do |category, preevent_products|
    - preevent_products.each do |preevent_product|
      h3.store-module-title = category.pluralize
      = render @checkout_presenter.draw_product(preevent_product), preevent_product: preevent_product
  .wrapper-store-module
=======
  = form_tag event_checkouts_path(current_event), method: :post do |f|
    - @checkout_presenter.preevent_products_hash.each do |category, preevent_products|
      h2.categories = category.pluralize
      - preevent_products.each do |preevent_product|
          h3.product-title = preevent_product.name
          = render @checkout_presenter.draw_product(preevent_product), preevent_product: preevent_product
>>>>>>> preevent_product_structure_implementation
    .checkout-total
      p.checkout-title = t('checkout.total')
      .checkout-amount.top-up-amount [data-id="checkout"]
        span [id="amount-total-checkout"] 0
        span #{current_event.currency}
    = button_tag t('checkout.button'), class: "btn btn-action btn-action-checkout"