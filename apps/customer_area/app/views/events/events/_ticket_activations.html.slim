h3 = t('dashboard.without_admissions.call_to_action')
p = t('dashboard.without_admissions.description')
= link_to t('dashboard.without_admissions.button'), new_event_admission_path(current_event), class: "btn"