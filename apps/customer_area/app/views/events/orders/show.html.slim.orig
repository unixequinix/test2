<<<<<<< HEAD
= t('orders.description')
table
  tr
    th = t('orders.products')
    th
    th = t('orders.price')
  - @order.order_items.each do |order_item|
=======
.wrapper-field.form-inputs
  p = t('orders.description')
  table.table-order
    tr.table-head
      th = t('orders.products')
      th
      th = t('orders.price')
    - @order.order_items.each do |order_item|
      tr
        td [colspan="2"] #{pluralize(order_item.amount, order_item.preevent_product.name)}
        td.table-amount #{order_item.total}
>>>>>>> preevent_product_structure_implementation
    tr
      td [colspan="2"] #{pluralize(order_item.amount, order_item.online_product.name)}
      td #{order_item.total}
  tr
    td [colspan="2"] = t('orders.total')
    td [colspan="3"] #{@order.total} #{current_event.currency}
= t('orders.vat')
= render 'events/shared/ssl_cert_seal'
= fa_icon "cc-visa"
= fa_icon "cc-mastercard"
= fa_icon "cc-amex"
= t('orders.redirect')
= button_to t('orders.button'), event_order_path(current_event, @order), method: :patch, remote: true, class: "btn"

#paymentForm class="wrapper-field form-inputs"
