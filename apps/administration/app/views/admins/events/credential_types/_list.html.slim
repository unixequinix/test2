.current-showing = t('helpers.page_entries_info.more_pages.display_entries_small', count: @list_model_presenter.current_items, total: @list_model_presenter.count)
table.credential_types
  thead
    tr
      th = t('admin.preevent_item.name')
      th = t('admin.preevent_item.description')
      th = t('admin.credential_type.position')
      th.table-actions-field = t('admin.defaults.actions')
  tbody
    - credential_types.each do |credential_type|
      tr
        td data-label=t('admin.preevent_item.name') = link_to(credential_type.preevent_item.name, edit_admins_event_credential_type_path(current_event, credential_type))
        td data-label=t('admin.preevent_item.description') = credential_type.preevent_item.description
        td data-label=t('admin.credential_types.position') = credential_type.position
        td.table-actions-field
          = render 'admins/events/credential_types/actions', credential_type: credential_type unless current_event.started?

= paginate credential_types