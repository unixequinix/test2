$("tbody").prepend("<%=j render('row', agreement: @agreement, company: @company) %>");
$('#company_event_agreement_event_id option:eq(<%= @agreement.event.id %>)')
  .prop('selected', true)
  .remove()
