<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= t('email.customer.order.completed.title') %></title>
  </head>
  <body style="margin: 0; padding: 0; background-color:#e6d500;">
    <table align="center" border="0" cellpadding="0" cellspacing="0" height="100%" style="border-collapse: collapse; margin: 0; background-color: #00DFA7 0%; text-align: center; font-family: sans-serif;" width="100%">
      <tr>
        <td>
          <table width="600px" align="center">
            <tr>
              <td colspan="2" style="text-align: center;">
                <%= image_tag attachments['logo.png'].url, :alt => 'BBF', style: 'display: inline-block; width: 350px; height: auto; margin: 0 auto;' %>
              </td>
            </tr>
            <tr>
              <td colspan="2" style="text-align: left; font-weight: bold;" >
                <%= t('email.customer.order.completed.greet', name: @name) %>
              </td>
            </tr>
            <tr>
              <td style="text-align: left;">
                <span><%= "#{t('email.customer.order.completed.order_number')}:" %></span>
                <span style="font-weight: bold;"><%= "#{@order.number}" %></span>
              </td>
              <td style="text-align: left;"><%= "#{t('email.customer.order.completed.date')}: #{@order.completed_at.strftime("%y-%m-%d %H:%M:%S")}" %></td>
            <tr>
              <td colspan="2" style="padding:24px 0; text-align: center;">
                <table width="600px" align="center" border="0" cellpadding="0" cellspacing="0" style="border-collapse: collapse; margin: 0 auto; background-color: #fff;">
                  <tr>
                    <th style="padding: 14px 0; background-color: #2c2c2c; color: #fff;"><%= t('orders.products') %></th>
                    <th style="padding: 14px 0; background-color: #2c2c2c; color: #fff;"><%= t('orders.price') %></th>
                  </tr>
                  <% @order.order_items.each do |order_item| %>
                  <tr>
                    <td><%= pluralize(order_item.amount, order_item.online_product.name) %></td>
                    <td><%= order_item.total %></td>
                    <% end %>
                  </tr>
                  <tr>
                    <td style="height:24px; vertical-align: middle;"><%= t('orders.total') %></td>
                    <td style="height:24px; vertical-align: middle;"><%= "#{@order.total} #{t('currency_symbol')}" %></td>
                  </tr>
                </table>
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>

