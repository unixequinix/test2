- breadcrumb :admin_ticket_type, @ticket_type
- provide :actions do
  = link_to t("actions.back"), admin_ticket_type_url(@ticket_type), class: "btn btn-action btn-action-back"
= render "form", ticket_type: @ticket_type